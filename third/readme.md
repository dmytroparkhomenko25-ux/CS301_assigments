# 1 - 42 рядки
Створюємо 5 таблиць для системи замовлень інтернет-магазину: customers, products, orders, order_items та order_log. Використовуємо DROP TABLE для запобігання конфліктам. customer_id, product_id, order_id, order_item_id та log_id мають тип SERIAL і є PRIMARY KEY. Налаштовано FOREIGN KEY зв'язки з каскадним видаленням для зв'язку замовлень із позиціями

# 44 - 60 рядки
Функція calculate_order_total(p_order_id INT). Рахує загальну суму замовлення на основі позицій у таблиці order_item як quantity * price. Повертає 0, якщо замовлення не має позицій

# 62 - 75 рядки
Процедура create_order(p_customer_id INT). Створює нове замовлення для клієнта. Робить перевірку на існування клієнта та викидає помилку (exception), якщо клієнт не знайдений. Ініціалізує суму замовлення як 0.00

# 77 - 124 рядки
Процедура add_product_to_order(p_order_id INT, p_product_id INT, p_quantity INT). Додає товар до замовлення. Робить перевірки: кількість має бути більшою за нуль, замовлення та товар мають існувати, а залишок товару на складі має бути достатнім. Зменшує stock_quantity після додавання товару.

# 126 - 152 рядки
Тригер trg_update_order_total та відповідна функція fn_update_order_total(). Спрацьовують після INSERT, UPDATE або DELETE на таблиці order_items. Автоматично перераховують та оновлюють total_amount у таблиці orders для відповідного замовлення.

# 154 - 170 рядки
Тригер trg_order_audit_log та відповідна функція fn_order_audit_log(). Спрацьовують після INSERT на таблиці orders. Автоматично записують подію створення замовлення до таблиці аудиту order_log

# 172 - end
Блок тестування. Створює тестового покупця, додає товар на склад, створює замовлення через процедуру, додає туди товари та перевіряє правильність спрацювання тригерів (зменшення залишку товару, оновлення суми замовлення та запис у лог)

# Аналіз

Аналіз виконання запиту (EXPLAIN ANALYZE) для пошуку товарів у замовленні №1:
```text
 Hash Join  (cost=27.09..41.32 rows=7 width=274) (actual time=0.048..0.052 rows=2 loops=1)
   Hash Cond: (p.product_id = oi.product_id)
   ->  Seq Scan on products p  (cost=0.00..13.00 rows=300 width=222) (actual time=0.008..0.009 rows=5 loops=1)
   ->  Hash  (cost=27.00..27.00 rows=7 width=28) (actual time=0.019..0.019 rows=2 loops=1)
         Buckets: 1024  Batches: 1  Memory Usage: 9kB
         ->  Seq Scan on order_items oi  (cost=0.00..27.00 rows=7 width=28) (actual time=0.006..0.008 rows=2 loops=1)
               Filter: (order_id = 1)
               Rows Removed by Filter: 3
 Planning Time: 0.956 ms
 Execution Time: 0.110 ms
```

## Деталі виконання запиту
1. **Seq Scan** на таблиці order_items виконує послідовне сканування для фільтрації записів із order_id = 1. Оскільки таблиця дуже маленька (5 рядків), сканування всієї таблиці є оптимальним рішенням для планувальника
2. **Hash** будує в оперативній пам'яті тимчасову хеш-таблицю для знайдених рядків з order_items
3. **Seq Scan** на таблиці products зчитує всі доступні продукти
4. **Hash Join** виконує з'єднання результатів за умовою p.product_id = oi.product_id, швидко підставляючи назви товарів до позицій замовлення через створену хеш-таблицю. Планувальник обирає Seq Scan замість Index Scan через мізерний об'єм тестових даних
